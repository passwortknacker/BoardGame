too weak cards: elemental surge, mana converter, alliance amulet (triggers before support cards can be used, so needs to be "on hand")
borderline too weak: source dynamo 
good: mana orb, 
borderline too strong: hexcores, 
too strong: 

bug: 2 cards calling each other infinitely from discard! Only allowed once.
  -> FIXED (sim, 2026-06-17, v0.4.1). Cause: two Weapon retrievers (e.g. 2x Arsenal Enforcer,
     each "Move 1 Weapon from Discard to Hand") ping-pong via play_hand's re-scan; ~299 plays /
     598 dmg in one turn (capped only by the 300 guard). Fix: a card may be pulled back from
     discard at most ONCE per turn (EffectContext.replayed_from_discard; from_discard +
     replay_weapon_from_discard now exclude+mark). Now bounds to 3 plays / 6 dmg.
     NOTE: web/game.js has the same retrieval ops but is human-paced (no auto-loop); apply the
     same 1x/turn cap there if it becomes exploitable in playtest.

--- Fixed this session (sim, 2026-06-17, v0.4.0-0.4.1) ---
- Eternal Charm self-refire infinite recursion (refire now marks an artifact before firing it).
- Double-remove crash when a refire consumes an artifact the turn-fire loop still holds
  (guarded with "eq in p.equipped").
- Infinite discard replay (above).